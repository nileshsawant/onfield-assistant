# Architecture

High-level layout, sourced from the repo root's `ARCHITECTURE.md`. For a
deeper walk through the runtime (RAG pipeline, memory system, tool-call
protocol, `--serve` internals), see the [technical overview](ofa-technical-overview.md).

```{include} ../ARCHITECTURE.md
:relative-images:
:heading-offset: 1
```
